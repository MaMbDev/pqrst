<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Pqrst" parent="Theme.AppCompat.DayNight.NoActionBar" />

    <!--
        Splash screen theme (AndroidX SplashScreen API, API 12+ back-compat to API 26).
        The activity starts with this theme; postSplashScreenTheme restores Theme.Pqrst
        once the splash is dismissed.
    -->
    <style name="Theme.Pqrst.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/pqrst_burgundy</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/pqrst_burgundy</item>
        <item name="postSplashScreenTheme">@style/Theme.Pqrst</item>
    </style>

</resources>
